<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.degree.abbylaura.savingstate.MainActivity">


    <TextView
        android:id="@+id/notesTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/notesTextViewText"
        android:textSize="20sp"
        />

    <EditText
        android:id="@+id/notesEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:lines="6"
        android:maxLines="6"
        android:hint="@string/notesEditText"
        android:gravity="top"
        android:inputType="textMultiLine"
        android:layout_below="@+id/notesTextView" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btnSettings"
        android:text="@string/btnSettingsText"
        android:layout_below="@+id/notesEditText"/>

</RelativeLayout>
